load "std.luna"

fun fib(i n a b) do
    if lt(i 0)
        error "index can't be negative"

    var tmp a
    set a b
    set b add(b tmp)
    
    if lt(i n)
        fib(add(i 1) n a b)
end

print(fib(1 1530 1 1))